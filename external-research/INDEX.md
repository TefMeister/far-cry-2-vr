# Research index

**Last `/gr` pass: 2026-09-23 (estate sweep) — CHECK-IN.** Indexed phunkaeg's *VR Modding Playbook* (two Far Cry 2 VR projects, the R1–R4 failures, store-build identity); row-by-row leads are in the modding drop of the same day.

_Previous: **Last `/gr` pass: 2026-09-11 (estate sweep) — FULL** (board `OPEN` block after the first headset run, INDEX, one delegated multi-source hunt across the stereo-3D and VR-injector communities)**.** Inbox empty. **One new topic, and its most valuable result is a CLOSED DIRECTION rather than an open one.** vorpX's developer states on the record that decoupling head rotation from the game camera is not possible in a FullVR style, and a user in the same 2020 thread describes our exact “ground and sky, nothing else” symptom — so the head rotation must reach Dunia's own camera, and no matrix edit will substitute. Both public projects that solved it are **hybrid** (coarse rotation into the game camera, residual correction at render time), and R.E.A.L. warns the game contests camera ownership each frame. The weapon row gets a four-rung ladder whose **second rung — a lower separation preset while aiming — is implementable now on a key we already own**, and is exactly what the one public FC2 stereo fix (DHR, 2013) did. ⭐ Three FC2-specific prior-art items: **vorpX DirectVR reportedly works here**, **weapons “not in scale” is already a known vorpX symptom in this game**, and **FoxAhead's Multi Fixer patches Dunia in memory at runtime** — precedent and collision check at once. ⚠️ **Still outstanding for the modding lane, seventh sweep running:** `engine-research/ENGINE-DOSSIER.md:382` still reads *“last activity 2019-11-23”* for issue #1253, which my own 2026-09-04 drop corrected to 2020; four drops now sit in that inbox about it._

_Previous: **Last `/gr` pass: 2026-09-10 (estate sweep) — CHECK-IN** (board `OPEN` block + INDEX; nothing searched)**.** Inbox empty. **Nothing new** — the board is down to two rows and both are `[VR @home]`: the AER stereo build is deployed on the home PC and one headset launch tests stereo and head tracking together, with the contingent per-eye-pose row behind it. Nothing static, nothing researchable. ⚠️ **Still outstanding for the modding lane, sixth sweep running:** `engine-research/ENGINE-DOSSIER.md` §AER still reads *"last activity 2019-11-23"* for issue #1253, which my own 2026-09-04 drop corrected to 2020; three drops now sit in that inbox about it._

_Previous: **Last `/gr` pass: 2026-09-07 (estate sweep) — CHECK-IN** (board `OPEN` block + INDEX; nothing searched)**.** Inbox empty. **Nothing new.** Board unchanged since 2026-09-04. The four commits since the last pass are all hygiene — a `/gs` drop, an `/sr` drop, and this lane's own two corrections (`verified-static` is not a vocabulary tag; the #1253 topic's head vs its correction section). Both open rows are `[VR @home]`._

_Previous: **Last `/gr` pass: 2026-09-05 (estate sweep) — CHECK-IN** (inbox drained — one `/gs` hygiene drop; the #1253 topic's opening paragraph and source line corrected, one INDEX phrase corrected)**.** **Inbox drained, nothing searched.** `/gs` was right that the topic's *head* still said *"last activity is the original report, 2019-11-23 — nearly seven years"* and *"no recorded fix"*, and that both are wrong. It **overstated one point**, though, and the correction is worth recording: the substantive half — the reporter's hedged lighthouse-only partial fix — **was already folded into the topic** on 2026-09-04, in its own section at the foot of the page. The real defect was narrower and arguably worse: **the page contradicted its own correction section 100 lines further down**, so which claim a reader believed depended on how far they scrolled. Opening paragraph now states the corrected dates, carries a pointer to the correction section, and the Sources line no longer repeats the creation date as the last activity. Also checked, as the drop asked: **`XIII2003-vr`'s OpenXR topic is clean** — its off-vocabulary-tag correction really was applied to the topic itself, not just noted in its index._

_Previous: **Last `/gr` pass: 2026-09-04 (estate sweep, third pass today) — CHECK-IN** (inbox drained — one `/sr` drop, verified independently; INDEX + the #1253 topic extended)**.** Both of `/sr`'s points check out against the GitHub API and all 8 comments: **last activity is 2020-04-22**, not the creation date, and the reporter himself reported a **lighthouse-only** partial fix in 2019, hedged and never in a changelog. **The estate-wide read changes the caution:** testing here happens on a Quest 3 over Virtual Desktop, so there is no lighthouse device to produce a misleading pass — a failure here is expected and weak, a pass would be the informative one. Dossier correction dropped to `engine-research/inbox/`; the same caution routed to `XIII2003-vr`, whose `[VR]` row tests exactly this._

_Previous: **Last `/gr` pass: 2026-09-04 (estate sweep) — CHECK-IN** (board OPEN block + INDEX)**.** Inbox empty. **Nothing new, and not searched** — the two rows (AER stereo `[PD]`, headset run `[VR @home]`) are unchanged and internal. My 2026-09-02 drop and the `/sr` 2026-09-03 drop are still in `engine-research/inbox/`, the modding lane to drain._
_Previous: **Last `/gr` pass: 2026-09-03 (estate sweep) — CHECK-IN** (board OPEN block + INDEX)**.** **No new research** — this pass corrected an off-vocabulary `[verified-static]` tag in the OpenXR per-view-pose topic (→ `[reported]`, first-party sourcing kept in prose). The two open rows are AER stereo work already recorded as unblocked, and a headset run. Noted for the next pass: `/sr` dropped `2026-09-03-sr-per-view-poses-are-honoured-differently-by-runtime.md` into `engine-research/inbox/` today — that is the modding lane's to drain, not mine, and it was left untouched._
_Previous: **Last `/gr` pass: 2026-09-02 (scoped re-run) — CHECK-IN** (board OPEN block + INDEX + a tracked-source re-check)**.** Inbox empty. This morning's pass recorded nothing new; this one re-checked the source the AER design depends on. **OpenVR #1253 is still open…_
_Previous: Last `/gr` pass: 2026-09-01 — FULL. Inbox drained (head-tracking verdict + the sweep's console lead). One new topic on the `devmodeon` console unlock; the head-tracking row is now ✅ incorporated._

Every research topic gathered for this project, newest first. Each row links to a self-contained
write-up in `topics/`. Status tags:

- 🆕 **new** — found, not yet acted on by the modding side.
- 👀 **reviewed** — a modding session has read it and factored it into a decision, but nothing shipped from it yet.
- ✅ **incorporated** — directly led to a real change (code, a test, a note) in one of the other five repos; linked below.
- ❌ **dead end** — checked out, didn't pan out; kept for the record so it isn't re-investigated from scratch.

| Date | Topic | Status | Summary |
| --- | --- | --- | --- |
| 2026-09-23 | [The VR Modding Playbook has two Far Cry 2 VR projects studied in depth](topics/2026-09-23-the-vr-modding-playbook-has-two-far-cry-2-vr-projects-studied-in-depth.md) | 🆕 | Camera-as-a-global predicts which bugs to expect; Steam = Ubisoft build proven by section hashes and PDB path. |
| 2026-09-11 | [Both ⭐⭐ rows have public prior art: vorpX's developer says the matrix alone CANNOT fix culling, and the weapon has a four-rung fix ladder](topics/2026-09-11-vorpx-says-the-matrix-cannot-fix-culling-and-the-weapon-has-a-fix-ladder.md) | 🆕 new | **Answers both ⭐⭐ `[PD]` rows from the first headset run.** ① **Culling:** the exact symptom is described verbatim on the vorpX forum (2020-05-08, *“by turning around you'd see either severely incomplete geometry or the void”*), and **Ralf, vorpX's developer, states decoupling head rotation from the game camera “isn't really possible … with the FullVR play style”** — so vorpX maps head rotation to **mouse movement** instead. ⭐ That **closes a direction**: there is no matrix edit waiting to be found. Routes are (a) synthetic mouse delta — cheap, guarantees culling, but welds aim to head and inherits the game's smoothing/accel (opentrack #113/#120/#803 document recentring spin, jitter, and 45° reading as 50–60°); (b) write the camera rotation in memory — what **vorpX DirectVR** and Vireio's **VRBoost** do; (c) relax culling — **peripheral slack only**, and RTX Remix's Anti-Culling docs state it cannot conjure draws the game never submitted. **Both working projects are HYBRID:** R.E.A.L. ties game camera yaw/pitch to the headset then corrects the residual at render time — and carries fixed-yaw handling for cases *“where the game wrestles for camera ownership”*, so the write must be re-asserted. ② **Weapon:** the stereo shift is `Δx ∝ Separation·(w − Convergence)` and `w` comes from whichever projection drew it — the viewmodel has its **own** FOV and near-plane, so no global figure serves both. Ladder: lower convergence (flattens the world) → ⭐ **hotkeyed low-separation preset while aiming** (what DHR's own 2013 FC2 3D-Vision fix did, keys `O`/`P`; DarkStarSword made it right-mouse-held in 2014) → per-draw override filtered by shader/index/texture hash (geo-11) → **move the weapon**: R.E.A.L. pushes it away and aligns it to the **dominant eye**; CryEngine 3 shipped `r_StereoNearGeoScale` to do the same. ⭐ **FC2-specific:** a 2020 vorpX user reports **DirectVR working in FC2** (bed saves only) **and weapons “not in scale”** — so a Dunia camera-rotation address is findable, and our weapon defect is already a known vorpX symptom. **FoxAhead's Far Cry 2 Multi Fixer** patches Dunia in memory at runtime — a precedent **and** a collision check. ⚠️ No published Dunia camera addresses anywhere; no FC2 weapon-doubling fix on record. |
| 2026-09-02 | [The SteamVR per-eye-pose bug is still open — and OpenXR is the way around it](topics/2026-09-02-the-steamvr-per-eye-pose-bug-is-still-open-and-openxr-is-the-way-around-it.md) | 🆕 new — **corrected + extended 2026-09-04:** last activity is **2020-04-22**, not the 2019-11-23 creation date `[verified-live 2026-09-04, n=1 API read]`; still open and still no Valve reply. **A partial fix was reported by the reporter himself (2019-12-17), lighthouse driver only, hedged, never in a changelog** — and since this estate tests on a **Quest 3 over Virtual Desktop**, the usual "a lighthouse test could pass and mislead" caution **cannot apply here**; instead a failure here is the expected, weak result and a pass would be the informative one. Design conclusion unchanged — **OpenXR half verified 2026-09-02** against Khronos's header; only runtime honouring is still open | Re-check of the source the AER submission design rests on: **OpenVR #1253 is still open, untouched since 2020-04-22**, no Valve response — so SteamVR discarding all but the last submitted per-eye pose is permanent, and the current plan (both eyes together, shared pose, double-buffered) is correct rather than merely tidy. Notable: the reporter is **LukeRoss00**, describing precisely this AER technique — the canonical implementer hit this wall and it was never fixed. **OpenXR's projection layer carries a pose per view**, so the collision does not arise in the same shape `[hypothesis]` — verify against the spec first. Cross-project: XIII already has an unverified OpenXR host. |
| 2026-09-01 | [`devmodeon` unlocks the console, and TAB enumerates it](topics/2026-09-01-devmodeon-unlocks-the-console-and-tab-enumerates-it.md) | 🆕 new | The console opens on `~` (**layout-dependent** — a Spanish-keyboard user reports `¶`), and the hidden commands are unlocked by typing **`devmodeon` in-console** — *not* the `-DEVMODE` launch flag, which is separately documented as unlocking all levels. 🪤 **`devmodeon` prints "Unknown command: devmode" when it works**, so verify by TAB-enumerating before and after and diffing, never by the message. `?`/`help` describe commands, TAB enumerates. Directly useful here: `gfx_DisableShadowGeneration` and `gfx_WaterReflectionQuality` are the exact knobs behind this lane's own shadow-cascade and water-reflection pass warnings; `gfx_MaxFPS` probes the vsync/compositor-cadence gotcha; `Exec` makes a stereo test repeatable. **Highest value: hunt the enumerated list for a camera or position readout** — it would be an independent check on the head-tracking position solve, obtainable with no headset. All `[reported]`, none tested on our build. |
| 2026-08-25 | [HelixMod Far Cry 2 corroborates pass inventory](topics/2026-08-25-helixmod-far-cry-2-corroborates-pass-inventory.md) | 🆕 new | A real 3D Vision fix for this exact game corroborates the shadow-cascade finding (shadow quality must stay Medium, not High) and flags water reflections and fire/smoke as separate, still-imperfect trouble passes worth expecting in this project's own override. |
| 2026-08-25 | [SteamVR frame timing + vsync gotcha](topics/2026-08-25-steamvr-frame-timing-vsync-gotcha.md) | 🆕 new | Valve's own frame-timing guidance (WaitGetPoses cadence, recommended loop order) plus a directly relevant gotcha: the game's own D3D9 Present must not vsync to the desktop refresh rate, or it fights the compositor's cadence independent of the already-planned double-buffer fix. |
| 2026-08-24 | [Head tracking: OpenVR pose-matrix layout + coordinate conversion + Vireio alternative](topics/2026-08-24-head-tracking-coordinate-conversion-and-alternate-technique.md) | ✅ incorporated | OpenVR's official `HmdMatrix34_t` layout reference (row-major storage, column-major read, a transpose gotcha, "B from A" direction convention). Dunia (Z-up/Y-forward) vs OpenVR (Y-up/−Z-forward) needs an axis remap with no universal pre-built matrix found — recommends deriving the target basis from the game's own already-parsed matrix each frame (extending the project's existing self-deriving approach) instead of hardcoding a guessed permutation. Also: Vireio Perception's VRBoost solves generic head-tracking by masquerading HMD rotation as mouse input, sidestepping coordinate conversion entirely — a documented fallback technique, rotation-only.**✅ Outcome 2026-09-01:** built as `staging/far-cry-2-vr/proxy-winmm/src/{stereo.c,vr_bridge.c}`, compile-clean and numerically verified, never run (no headset on the dev machine). **The §2 recommendation was followed and was right** — no permutation table in the code; the basis is derived from `M`'s own rows every frame and the conversion reduces to one change of basis `B·H·Bᵀ`, with OpenVR's +Z-backwards folded into `B`. §1's layout warning was used as written. **The two bugs that actually appeared were not convention bugs:** a position solve mixing normalised basis rows with raw translation terms, and a rotation composed as the *camera* rotation where `M` wants its *inverse* — and that second one **presents exactly like the handedness problem §2 warns about**, so the obvious knob would have hidden it. Caught offline by a numerical harness (relative error 3.4 → ~1e-15). §3's Vireio input-masquerade fallback is **not adopted, not dead** — the matrix route gives real 6DOF including position, which mouse masquerade cannot express. |
| 2026-08-24 | [AER/SteamVR ghosting + CPU readback & shared-surface techniques](topics/2026-08-24-aer-steamvr-ghosting-and-cpu-readback-techniques.md) | 🆕 new | SteamVR has a known bug that discards one eye's pose on alternate-eye `Submit` — validates the planned double-buffer-both-textures approach rather than racing per-eye pose timing. Also: the standard double/triple-buffer fix for `GetRenderTargetData` stalls, the official D3D9Ex→D3D11 shared-surface interop technique (with a keyed-mutex incompatibility gotcha specific to D3D9), the standard `W = 2·Z·tan(θ/2)` matched-FOV virtual-screen formula for the next roadmap item, and confirmation that some AER ghosting is an industry-wide known tradeoff, not a bug to chase. |

## How to add a topic

1. New file in `topics/`, named `YYYY-MM-DD-short-slug.md`.
2. One row added to the table above, newest at the top.
3. Update the status tag here as it moves through review → incorporated/dead-end (the modding side should update this when it acts on a lead, so the index reflects reality without the research side needing to poll).
